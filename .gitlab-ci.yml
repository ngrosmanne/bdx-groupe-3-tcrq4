image: docker:latest
services:
  - name: docker:dind
    alias: docker
    
stages:
- test

docker:test:
  stage: test
  tags:
    - docker
  before_script:
    - docker --version
  script:
    - docker ps